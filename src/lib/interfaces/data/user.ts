export interface IUser {
	id: number;
    name: string;
}

export interface IUserDetail {
	id: number;
	name: string;

	// Here we need to add all of the
	// additional details which will show
	// on the individual user page
}
