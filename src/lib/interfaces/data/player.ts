export interface IPlayer {
	id: number;
    name: string;
}

export interface IPlayerDetail {
	id: number;
	name: string;

	// Here we need to add all of the
	// additional details which will show
	// on the individual player page
}
