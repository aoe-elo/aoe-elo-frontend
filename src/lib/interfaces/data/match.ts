export interface IMatch {
	id: number;
    name: string;
}

export interface IMatchDetail {
	id: number;
	name: string;

	// Here we need to add all of the
	// additional details which will show
	// on the individual match page
}
